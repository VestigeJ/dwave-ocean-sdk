[build-system]
requires = [
    "setuptools>=46.4.0",
    "wheel",
]
build-backend = "setuptools.build_meta"

[tool.ruff]
exclude = [
    ".git",
    ".github",
    ".circleci",
    "docs",
    "tests",
    "venv",
    ".venv",
]

[tool.ruff.lint]
select = ["S"]
